# Task 07 — Forms

## Objective
Understand and practice:
- Form structure
- Text, email, password inputs
- Radio buttons and checkboxes
- Dropdown/select
- Textarea
- File input
- Number input with validation
- Fieldset & legend grouping
- Input validation attributes (required, minlength, min, max, accept)

## Files
- index.html

## Steps I performed
1. Created a complete form with various input types.
2. Added client-side validation rules (`required`, `minlength`, etc.).
3. Used `<fieldset>` and `<legend>` for grouping radio/checkbox sections.
4. Styled the form for readability.
5. Tested form behavior in browser.

## Git Commands
```bash
git checkout -b html-task-07-forms
git add HTML/07-forms
git commit -m "HTML Task 07: Created complete form with validation and README"
git push --set-upstream origin html-task-07-forms
