# JS Task 02 — Events

## 🔥 Concepts Covered
- Click event: `addEventListener("click")`
- Keyup event: runs every time you type
- Mouse events:
  - mouseenter
  - mouseleave
- Updating DOM with JS (`innerText`)
- Logging values using console

## 📁 Files
- index.html — contains UI elements
- script.js — contains event handling logic

## 🧪 What This Task Demonstrates
- Clicking a button updates the page
- Typing inside input shows real-time text
- Mouse movements trigger visual feedback
- Practical use of event listeners

## 🧩 Git Workflow
```bash
git checkout -b js-task-02-events
git add JS/02-events
git commit -m "JS Task 02: Added event handling examples"
git push --set-upstream origin js-task-02-events
