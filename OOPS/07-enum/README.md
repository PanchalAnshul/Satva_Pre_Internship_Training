# Task 07 — Enum (Java)

## Objective
Use enum to represent constants with readability. Learn ordinal, name, custom values, and switch on enums.

### Enum
- declare enum
- convert enum to int and back
- use in switch
