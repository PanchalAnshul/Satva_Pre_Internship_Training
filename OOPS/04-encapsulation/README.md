# Task 04 — Encapsulation (Java) 

## Objective
Hide internal data and expose safe operations using private fields + getters/setters.

## Encapsulation
- private fields
- public getters/setters with validation
- internal private helper methods
- only expose safe public API
