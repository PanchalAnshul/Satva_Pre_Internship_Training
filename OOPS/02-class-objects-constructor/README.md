# OOPS Task 02 — Class, Objects & Constructors (Java)

## 🎯 Objective
Learn how to define a class, add fields/properties, create methods, use constructors (default & parameterized), and create objects in Java.

---

## 🔑 Key Points
- **Class**: blueprint (defines fields and methods).
- **Object**: instance of a class created with `new`.
- **Field**: class variable (use `private` for encapsulation).
- **Getter/Setter**: `public` methods to access private fields.
- **Constructor**: special method called when creating objects.
  - Default (no-arg) constructor.
  - Parameterized constructor.
- **this**: keyword to refer to current object.

---

## Files
- `Main.java` — contains `Student` class + demo `main` method.

## How to run (VS Code / Terminal)
1. Open folder `OOPS-Java/02-class-objects-constructor` in VS Code.
2. Save `Main.java`.
3. In terminal:
   ```bash
   javac Main.java
   java Main
